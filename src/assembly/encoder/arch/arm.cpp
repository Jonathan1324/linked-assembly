#include "arm.hpp"

namespace arm {
    size_t encodeInstruction(Instruction& instr, EncodedSection& section)
    {
        (void)instr;
        (void)section;
        //TODO
        return 0;
    }
}