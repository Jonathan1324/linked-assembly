name: build-test-windows

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: windows-2025

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install build dependencies
        run: python scripts/install_build_deps.py
        shell: bash

      - name: Build the project
        run: python -m ci.ci --no-test -a -A linked-assembly-windows-x86
        shell: bash

      - name: Upload binaries as artifact
        uses: actions/upload-artifact@v4
        with:
          name: linked-assembly-windows-x86
          path: dist/

      - name: Upload logs as artifact
        uses: actions/upload-artifact@v4
        with:
          name: linked-assembly-windows-x86-logs
          path: logs/
