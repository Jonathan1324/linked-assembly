toolchains:
  myToolchain:
    description: This is a test toolchain

environments:
  abc:
    description: Test desc
    toolchain: myToolchain
    vars:
      SRC_DIR: src
      BUILD_DIR: build

targets:
  test:
    path: ${env.SRC_DIR}
    config: build.yaml
    outputs:
      main:
        type: executable
    env: abc
